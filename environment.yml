name: occ-bridge-dev
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip

  - pythonocc-core>=7.6.0
  - numpy>=1.21.0

  - pybind11>=2.10.0
  - scikit-build-core>=0.8.0
  - cmake>=3.18
  - ninja
  - python-build

  - pytest